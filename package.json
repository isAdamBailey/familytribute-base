{
    "private": true,
    "scripts": {
        "build:frontend": "[ -n \"$E2E_BASE_URL\" ] || npm run build --prefix frontend",
        "test:e2e": "npm run build:frontend && env -u NO_COLOR playwright test",
        "test:e2e:ui": "npm run build:frontend && env -u NO_COLOR playwright test --ui",
        "test:e2e:repeat": "npm run build:frontend && env -u NO_COLOR playwright test --repeat-each=3"
    },
    "devDependencies": {
        "@playwright/test": "^1.61.1",
        "@types/node": "^22.20.1",
        "otpauth": "^9.5.1",
        "typescript": "^5.9.3"
    },
    "type": "module"
}
